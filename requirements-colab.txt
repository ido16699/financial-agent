# Colab-friendly deps. Compatible with Colab's Python 3.12 + numpy 2.x default.
# We allow numpy 2 so we don't fight the pre-installed runtime.
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.4
yfinance>=0.2.50
pyarrow>=15.0
click>=8.1
pydantic>=2.0
pydantic-settings>=2.0
PyYAML>=6.0
tqdm>=4.65
